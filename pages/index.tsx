import { Box } from '@mui/system';
import type { NextPage } from 'next';

import AppLayout from '../component/AppLayout';

const Home: NextPage = () => {
  return (
    <div>
      <AppLayout></AppLayout>
    </div>
  );
};

export default Home;
